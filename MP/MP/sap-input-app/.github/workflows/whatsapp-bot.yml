name: Automated WhatsApp Screenshot

on:
  schedule:
    # 02:00 UTC = 09:00 WIB
    # 08:00 UTC = 15:00 WIB
    - cron: '0 2,8 * * *'
  workflow_dispatch: # allow manual trigger

jobs:
  send-report:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4
      
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          
      - name: Install Dependencies
        run: npm ci
        
      - name: Run Puppeteer Script
        env:
          # User requested full automation without manual secret setup
          WEB_NIK: '19010048'
          WEB_PASS: 'ikatanistripenerbanganindonesia'
          GOWA_URL: 'https://gowa.waterflai.my.id'
          GOWA_USER: 'admin'
          GOWA_PASS: 'Sedap321#'
          TARGET_PHONE: '081251334618'
        run: node scripts/send_wa_image.js
